[project]
name = "seb"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "tabulate>=0.9.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.4",
]
